import React from 'react';
import { Homepage as HomepageComponent } from '../../components';

const Homepage = (): JSX.Element => {
  return <HomepageComponent count={1} />;
};

export default Homepage;
